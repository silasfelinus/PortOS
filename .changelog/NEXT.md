# Unreleased

## Apps

- **The per-app Jira Kanban board now shows your full workflow lifecycle, not just To Do / In Progress / Done.** The board used to collapse every ticket into Jira's three built-in *status categories*, so distinct workflow stages like **Blocked** and **In Review** (both "In Progress" to Jira) were invisible — they all piled into one column. The board now resolves the project's real workflow stages and renders one column per stage. When the app's Jira config has a `boardId`, PortOS reads that agile board's actual column layout from Jira (`/rest/agile/1.0/board/{id}/configuration`), so the columns and their order mirror exactly what you see on the board in Jira — including custom stages. Without a board id it falls back to the project's distinct statuses ordered by category, and if neither can be read it falls back to the original three-category board. Columns are color-cued (Blocked → red, Review/QA → purple, otherwise by category), and drag-to-transition now targets the exact status of the column you drop into (any ticket whose status maps to no column gets its own column so nothing disappears). The board now also spans the **full width** of the app Overview tab (the surrounding details stay capped for readability) so all lifecycle columns are visible across the desktop instead of being clipped at ~1024px, falling back to horizontal scroll only when there are more stages than fit. (`server/services/jira.js`, `server/routes/jira.js`, `client/src/components/KanbanBoard.jsx`, `client/src/components/apps/tabs/OverviewTab.jsx`)

## Chief of Staff

- **Feature Agents moved from Dev Tools to the Chief of Staff section.** The Feature Agents page (`/feature-agents`) now lives under the Chief of Staff sidebar group alongside the other agent-management pages, rather than buried in Dev Tools. The route is unchanged — only the sidebar grouping and the ⌘K / voice nav section label moved. (`client/src/components/Layout.jsx`, `server/lib/navManifest.js`)

- **The CoS → Schedule page is now a scannable card grid instead of a flat expandable list.** Each task type renders as a status card you can read at a glance — a colored status dot (active / on-demand / waiting-on-deps / disabled), a **next-run countdown** ("in 3h", "Mon 9:00") that was previously computed server-side but never shown, an app-coverage bar with the enabled/total count kept front-and-center, and last-run/run-count. Cards sort by status then soonest next run (so what's about to run rises to the top), the All/Enabled toggle is replaced by per-status filters (Active / On-Demand / Waiting / Disabled), and a name filter box jumps to a task quickly. A **Run** button sits on each card — for app-targeting tasks it opens an app picker so the on-demand run carries app context (just like the old per-task controls), and falls back to a plain global run for tasks with no managed apps; **Configure** opens a slide-over drawer with the full per-task settings (interval, provider/model, prompt, agent options, dependencies, pipeline stages, and per-app overrides) — the same controls as before, now in a focused panel that's deep-linkable via `?task=` and closes on Esc/backdrop. Mobile collapses the grid to a single column.

- **[issue-1563] Every Chief-of-Staff agent now records which machine produced it.** As a first step toward federating CoS agent history across peers, each spawned agent stamps the producing instance's federation identity (`instanceId`) into its metadata at registration. The stamp flows through to the agent's archived `metadata.json` automatically, so once agent history syncs between a node pair, each agent — and the worktree branch it created — can be attributed to the instance that ran it. This is the provenance prerequisite for the claim/lease ownership work that lets two federated peers share a backlog without ever duplicating each other's agent runs. (`server/services/agentLifecycle.js`)

- **[issue-1563] Two federated machines can now share a Chief-of-Staff backlog without ever running the same task twice.** Each task a CoS agent starts is now stamped with a time-bounded ownership *claim* — which machine took it, when, and a lease that stays alive while the agent works. Before starting any task, a machine checks the claim: if its peer holds a live lease, it backs off instead of spawning a second agent (which previously would have created conflicting worktrees and branches on the same repo and raced the recovery sweep). The lease auto-renews while the work runs and is released the moment the task finishes or fails back to the queue, and orphan recovery leaves a peer's in-flight task alone rather than resetting it. If a machine crashes mid-task its lease simply expires and the peer is free to pick the task up. The worktree each agent creates is also tagged with its origin machine. This is the safety groundwork that lets CoS be included in full peer mirroring (#1561). (`server/services/cosTaskClaim.js`, `server/services/agentLifecycle.js`, `server/services/cos.js`, `server/services/cosTaskStore.js`, `server/services/worktreeManager.js`)

## Local LLMs

- **The Hugging Face model search now lets you pick a quantization, and defaults to the highest-fidelity build that fits your machine's memory.** Previously every GGUF repo installed a single auto-chosen Q4-ish quant, hiding larger variants (like a `…-BF16.gguf`) that a big-memory box can comfortably run. Each Hugging Face result now carries a **Quant** menu listing every GGUF variant in the repo (multi-part shards summed into one entry), with a per-quant fit hint — *fits comfortably* / *tight fit* / *exceeds RAM* — measured against your usable memory. The default selection is RAM-aware: PortOS reserves headroom for the OS and KV cache, then picks the largest variant that still fits (so a 128 GB unified-memory Mac defaults to BF16/Q8 where a 16 GB laptop stays on Q4), and you can override it per model. This is purely a file-picker improvement — these are all still GGUF files the existing Ollama/LM Studio runtimes already support; nothing about the GGUF-only requirement changed (raw safetensors/full-precision HF models need a different runtime and remain out of scope). Ollama installs via `hf.co/<repo>:<quant>`; LM Studio via `<repo>@<quant>`.
- **[issue-1537] The Hugging Face search now surfaces Apple MLX models on Apple Silicon, not just GGUF.** On an Apple-Silicon Mac with LM Studio, search results now include MLX builds (Apple's native format — often the fastest path on unified-memory Macs) alongside GGUF, each tagged with an **MLX**/**GGUF** badge so you know what you're installing. MLX cards carry the same size + RAM-fit hint as GGUF cards and install through LM Studio (`lms get <repo>`). MLX is shown only where it actually runs: it's hidden on non-Apple hosts, and for the Ollama backend (Ollama's MLX acceleration applies to its own registry's GGUF models automatically and can't pull arbitrary Hugging Face safetensors repos).
- **The built-in "Recommended" model cards now have the same RAM-aware quant picker as the Hugging Face search.** Recommended models used to hard-code one quant each; cards backed by a Hugging Face repo now show the **Quant** menu and default to the highest-fidelity build that fits this machine's memory, so the recommendation adapts to your hardware instead of being fixed.
- **[issue-1539] Recommended models that pull from Ollama's own registry by bare name now get the RAM-aware quant picker too.** Previously only Hugging Face-backed recommended cards offered a **Quant** menu; cards for Ollama registry models (Llama 3.2, Qwen2.5, GPT-OSS, Llama 3.3, …) showed a single hard-coded build. PortOS now discovers each of those models' available quantizations from the Ollama registry and presents them in the same picker, with per-quant fit hints and the highest-fidelity build that fits your memory selected by default — so every recommended card adapts to your hardware, regardless of which registry the model comes from. When a model can't be reached on the registry, its card keeps its single curated build as before.
- **Hugging Face search results now rank by downloads and recency more strongly.** A model's download count and how recently it was updated now dominate the result ordering (recency is graduated — a model updated this week outranks an equivalent one from months or years ago, and stale models are demoted), so popular, actively-maintained models surface first instead of being outweighed by publisher/format bonuses.
- **[issue-1488] Pinning a small-context local model to a heavy analysis stage no longer overflows its window.** The reverse-outline, continuity-bible, comic-script verification, alternate-POV rewrite, and per-issue editorial-analysis stages used to size the manuscript they send to a fixed 48–60K-character floor regardless of the model — fine on a big-context provider, but on a genuinely small-window local model (e.g. an 8K-context model) that floor plus the prompt could exceed the window. These stages now scale the manuscript to the model's actual window, trimming it to fit a small one (never down to empty — a usable minimum is always kept) while still feeding the whole manuscript to a large-context model exactly as before.
- **Installing Ollama (or LM Studio) via Homebrew no longer reports a false "install failed."** `brew install` routinely exits non-zero *after* a successful pour — a failed `brew cleanup`, a dependency's post-install caveats, or env-hint noise (notably Ollama's new `mlx`/`mlx-c` dependencies) all bubble up as exit 1 even though the formula/cask is fully installed on disk. PortOS treated that exit code as a hard failure and told you the install didn't work, when in fact it did. The install path now verifies presence-on-disk via `brew list` after a non-zero exit and treats the package being installed as the source of truth — so a successful-but-noisy Homebrew install is reported as success and proceeds to start the backend, instead of surfacing a misleading error. (`server/services/localLlm.js`)
- **Clicking "Run at Startup" for Ollama no longer fails with `Bootstrap failed: 5: Input/output error`.** Registering Ollama as a login service runs `brew services start ollama`, which shells out to `launchctl bootstrap` — and that fails with launchd error 5 (EIO) when a stale service registration is already loaded (left over from a prior `ollama serve`, an interrupted start, or a Homebrew upgrade). PortOS now detects that specific bootstrap conflict, boots out the stale registration (`brew services stop`), and retries the start once — so the toggle succeeds instead of surfacing the raw launchctl error. Unrelated start failures (e.g. permission errors) are not retried. (`server/services/ollamaManager.js`)

## Comic pages

- **Comic pages now carry scene markers, and page renders chain within a scene but start fresh across a scene cut.** Scene boundaries are established once at the beat-sheet stage as a canonical `## Scenes` list (numbered `INT./EXT. LOCATION — TIME` sluglines) and flow down through the prose draft into the comic script, which now tags each page header `## Page N — Scene M: SLUGLINE`. The Comic tab shows the scene on every page, and the parser records `sceneNumber`/`sceneHeading` per page. The payoff is in rendering: the consistency-reference picker now defaults to **auto**, which uses the *previous* page as an image reference only when that page is in the **same scene** and already rendered — so incidental, un-described characters and the environment stay consistent through a continuing scene, while the first page of a new scene renders clean instead of dragging the prior scene's look across the cut. You can still override per page: **none** forces a fresh render mid-scene, or pick **prev/next page** to force a specific reference (e.g. to fix a character-consistency drift across a scene boundary). Pages with no scene markers (legacy comics) behave exactly as before — no auto-chaining. (Series Autopilot's page renders inherit the same within-scene continuity automatically.)
- **[issue-1534] Refine a rendered comic page with a small fix instead of re-rendering it from scratch.** Each rendered page now has a **Refine** box: type a free-text adjustment ("warm the lighting", "remove the extra signage text", "make the two background figures match across panels") and the page re-renders image-to-image from its *existing* image, applying only that change while preserving everything that was already right. The AI tweaks the page's current render prompt to reflect your instruction (it does not regenerate the prompt from the comic script), then runs a low-denoise re-render off the page's own image — so the panel layout, composition, and lettering survive. It refines the page's final render when one exists, otherwise its proof, and shows a short "what changed" summary after each refine. Works with both the local and Codex image backends.

## Storyboards

- **[issue-1552] Storyboard scenes now line up exactly with comic-page scenes.** The beat sheet's canonical `## Scenes` list is now the single source of truth for storyboard scene numbers and sluglines too. When you click "Generate scenes from prose/teleplay" on an issue whose beat sheet carries a `## Scenes` list, PortOS still extracts the rich per-scene content (dialogue, visual prompts, shots) from the prose/teleplay — so downstream audio voice-over and image/video generation keep everything they need — but overlays the canonical scene numbers and `INT./EXT.` sluglines on top, so "Scene 2" is the same Scene 2 across the beat sheet, prose, teleplay, comic script, comic pages, and storyboards. (For an issue with a scene list but no prose/teleplay yet, the scenes come straight from the list.) Issues with no canonical scene list fall back to the existing model-driven extraction exactly as before. The TV teleplay stage also now reuses the canonical scene numbers and sluglines verbatim when the source material carries them, so teleplays stay aligned with every other adaptation.

## ChatGPT import

- **[issue-1527] A failed ChatGPT export import no longer leaves a stray temp file behind under heavy disk load.** When an import was aborted mid-stream (e.g. a corrupt asset-name map or an oversized member), the cleanup that removes the half-written `.part` temp file could run *before* the asset still streaming to that file finished closing — so under heavy concurrent disk I/O the write could re-create the `.part` file just after it was deleted and orphan it. Cleanup now waits for every in-flight asset write to close before removing temp files, so a rejected import reliably leaves nothing behind.

## Apple Health import

- **[issue-1532] Apple Health exports that include clinical records now import instead of crashing.** Importing a Health export ZIP that contained a `clinical_records/` folder (the FHIR records you get after linking a healthcare provider) failed outright — the import aborted while reading those JSON files. Such exports now import correctly, with their clinical records loaded alongside the rest of your health data; exports without clinical records are unaffected.

## Per-issue story generation

- **[issue-1511] Per-issue prose and comic-script generation now sees only the characters that issue involves.** Generating an issue used to load the entire series character bible into every prose and comic-script prompt — on a large-cast series that's tens of thousands of tokens of character detail (including one-off bit players from unrelated issues) re-sent on every issue and every stage. Generation now sends full character records only for the cast named in that issue's beats, synopsis, and source material, with a compact one-line roster of everyone else kept for continuity. This sharply cuts token cost on big-cast series and, on context-bounded providers, leaves more room for the actual draft. The series' lead/recurring characters stay in context for every issue, so the core cast is never dropped — generation just stops carrying the full record of every bit player from unrelated issues.
- **[issue-1529] A character whose name is a common word (Will, May, Grace) is no longer pulled into a prompt by an incidental word.** When scoping an issue's character bible, an ordinary phrase like "the team **will** regroup" could be mistaken for naming a cast member called "Will" — incidentally scoping the prompt down to that one character. A single-word character name now only counts as named when it appears capitalized as a proper noun ("Will entered"), so common-word names match the same way every other name does. Multi-word names and nicknames are unaffected.

## Editorial checks

- **[issue-1581] A new editorial check catches timeline and canon contradictions — a dead character who reappears alive, an age that contradicts the bible, an impossible chronology.** PortOS had no editorial check for internal contradictions against canon and the story's own timeline, so a character killed in issue 3 who walks back in alive in issue 6, an age the prose contradicts (the bible says 16, the page says "in her 30s"), or a journey that takes eight days but is dated to day two could slip through to the reader — the kind of mistake that's nearly impossible to catch by eye once an arc spans months of drafting. The new **Timeline / canon contradiction** check (`continuity.timeline-contradiction`) reconciles the stitched manuscript against the **continuity-bible facts ledger** (the ground-truth ages, dates & elapsed time, locations, and world rules already extracted by the continuity bible), the canon character facts (age, role, status, description), the reverse-outline scene ordering (for chronology), and the authored per-character arcs — flagging each contradiction with the two facts that conflict and a verbatim anchor. It runs across the whole manuscript — carrying each character's last-known state (alive/dead, age, location) forward between chunks so a death-then-resurrection that spans chapters is actually visible — and degrades to a prose-only scan when no canon or outline exists. It's a default-on series check that surfaces in the existing editorial-review UI and Series Autopilot like every other check, marking plot-breaking contradictions high-severity per finding. Its findings go stale (and re-run) when any source it reads drifts — including the continuity-bible ledger — via the new `continuityBible` editorial source. (`server/lib/editorial/checkRegistry.js`, `server/services/pipeline/editorial/checkRunner.js`, `data.reference/prompts/stages/pipeline-editorial-timeline-contradiction.md`, `scripts/migrations/129-editorial-timeline-contradiction-stage.js`)

- **[issue-1582] A new editorial check catches unearned personality shifts — a reserved character who suddenly cracks jokes, a stated fear silently ignored, knowledge that appears with no on-page learning.** PortOS's `arc.transitions` check finds *change moments* but nothing flagged a change the story never *earns*: a guarded character trading easy banter a chapter later with no beat to motivate it, an established trait (a fear, an allergy, a skill) quietly contradicted, or a POV character who acts on information they were never shown learning. These read as author slips rather than growth and are hard to catch by eye once an arc spans months of drafting. The new **Character consistency** check (`character.consistency`) reconciles the stitched manuscript against the established canon character *traits* (personality, fixed traits, mannerisms, likes/dislikes, speech), the reverse-outline scene ordering (to spot a knowledge jump within a scene), and — crucially — the **authored per-character arcs**, so an intentional, earned transition the author recorded is *not* flagged. It runs across the whole manuscript, carrying each character's established temperament forward between chunks so a baseline set in an early chapter and an unearned shift in a later one are visible together, and degrades to a prose-only scan when no canon or outline exists. It's a default-on series check that surfaces in the existing editorial-review UI and Series Autopilot like every other check, marking trait contradictions that break a plot beat high-severity per finding. (`server/lib/editorial/checkRegistry.js`, `data.reference/prompts/stages/pipeline-editorial-character-consistency.md`, `scripts/migrations/130-editorial-character-consistency-stage.js`)

- **[issue-1583] A new editorial check catches a weak climax — a protagonist who is rescued at the payoff scene, or a climax that wins the plot but never lands the story's emotional/thematic core.** Nothing checked that the climax is the protagonist's *hardest, most active* moment: a finale where an ally swoops in to resolve the conflict, the antagonist conveniently self-destructs, or a coincidence settles things — or a climax that lands the action but leaves the theme the story has been building unanswered — could ship unflagged, even though a passive climax is story-breaking for the reader who invested in the whole arc. The new **Climax / resolution power** check (`arc.climax-agency`) identifies the climax scene and judges two things: *agency* (does the protagonist drive the resolution through a hard, costly choice, vs. being a spectator at their own climax) and *resolution power* (does the climax resolve the core problem and land the emotional/thematic argument, not just the surface plot). It reconciles the prose against the authored reader-map payoffs (what the reader was promised) and the declared themes, using the reverse-outline scene map to locate the climax, and gates its verdict on the final manuscript part — the climax can only be judged once the whole story is in view — degrading to a whole-manuscript scan when no reader-map, themes, or outline exists. It's a default-on series check that surfaces in the existing editorial-review UI and Series Autopilot like every other check, marking a fully passive climax high-severity; it complements `plot.structure-momentum` (which flags a passive protagonist arc-wide) by focusing the lens on the single payoff scene. (`server/lib/editorial/checkRegistry.js`, `data.reference/prompts/stages/pipeline-editorial-climax-agency.md`, `scripts/migrations/131-editorial-climax-agency-stage.js`)

- **[issue-1584] A new editorial check catches emotional beats that don't match the stakes — a trauma the story never processes, or a minor setback that triggers wildly outsized grief.** Nothing checked that reactions track event magnitude: a character could lose their brother in issue 2, the prose moves on, and the wound is never touched again — or a small disappointment could trigger a chapter of despair the story hasn't earned — and either way the emotional authenticity collapses without being flagged. The new **Emotional beat proportionality** check (`emotion.reaction-proportionality`) reads the stitched manuscript with the reverse-outline scene map to weigh each event's magnitude and flags two mismatches: an *under-reaction* (a high-magnitude event — death, trauma, betrayal, major loss or win — that draws no on-page reaction and is never processed in later issues) and an *over-reaction* (a minor setback triggering grief, rage, or despair out of all proportion). Because an unprocessed event in an early issue can stay unaddressed many issues later, the check carries every high-magnitude event still awaiting a proportionate reaction forward between manuscript chunks, so a trauma introduced early and then ignored is flagged against the later issue that should have paid it off; it degrades to a whole-manuscript scan when no outline exists, and respects characterization the story has established (a deliberately stoic or numb character isn't flagged for measured restraint). It's a default-on series check that surfaces in the existing editorial-review UI and Series Autopilot like every other check, marking a major trauma left wholly unprocessed high-severity. (`server/lib/editorial/checkRegistry.js`, `data.reference/prompts/stages/pipeline-editorial-reaction-proportionality.md`, `scripts/migrations/132-editorial-reaction-proportionality-stage.js`)

- **[issue-1585] A new editorial check catches flat secondary characters — a recurring side character who appears scene after scene but is exactly the same at the end as at the start.** The existing `pov.justified` check asks whether each *POV* character earns their viewpoint, but nothing judged the *non-POV* cast — so a recurring side character (a partner, a foil, a co-worker who shows up across half the book) could stay completely static, draining the story's texture, without ever being flagged. The new **Secondary-character arcs** check (`character.secondary-arc`) tallies the recurring non-POV cast from the reverse-outline scene map — characters present across multiple scenes who never hold the viewpoint — and judges whether each shows meaningful change, flagging a *flat arc* (the same situation, attitude, and standing at the end as the start) or a *purposeless regression* (an arc that quietly undoes itself with nothing in the story making it meaningful). It reads the stitched manuscript with the recurring-cast roster and the canon roster, deliberately ignoring genuine walk-ons (a one- or two-scene bit player owes the reader no arc) and a deliberately-static foil whose constancy is the point. Because a flat arc is a whole-story claim, it carries each recurring secondary's established state forward between manuscript chunks and gates its verdict on the final part — so a character established early whose change lands in a chapter no longer in view isn't falsely flagged — and degrades to a whole-manuscript scan when no outline exists. A configurable **recurring threshold** (default 2 scenes) tunes how prominent a character must be before it's held to an arc. It's a default-on series check that surfaces in the existing editorial-review UI and Series Autopilot like every other check, marking a prominent recurring character left wholly flat medium-severity. (`server/lib/editorial/checkRegistry.js`, `data.reference/prompts/stages/pipeline-editorial-secondary-arc.md`, `scripts/migrations/133-editorial-secondary-arc-stage.js`)

- **[issue-1586] A new editorial check catches narrator-voice whiplash — narration that's witty in issue 1, grim in issue 3, then witty again in issue 5.** The existing `dialogue.voice-distinctiveness` check guards each *character's* dialogue voice, but nothing watched the *narrator's* voice across a series — so the storytelling tone could lurch from issue to issue (a wry, ironic narrator turning flat and clinical, a spare hard-boiled voice going lush and lyrical) with no one flagging the inconsistency, even though voice consistency is part of the promise to the reader. The new **Narrative voice / tone consistency** check (`style.voice-consistency`) fingerprints each issue's narrative tone — diction, register, humor, emotional temperature, sentence rhythm — and flags two problems: a *cross-issue tonal shift* the story never earns, and *drift from the series style guide's intended voice* (the declared `tone` words) when one is set. It reads the stitched manuscript with the style guide's intended tone, deliberately ignoring a purposeful modulation the events justify (a grim chapter because the plot took a grim turn) and judging the narration only, not character dialogue. Because the comparison spans issues, it carries each issue's tone fingerprint forward between manuscript chunks, so a later issue's narration is judged against the voice the series established earlier; it degrades to a pure cross-issue consistency scan when no style guide tone is declared. It's a default-on series check that surfaces in the existing editorial-review UI and Series Autopilot like every other check, marking a sharp, unexplained whiplash medium-severity. (`server/lib/editorial/checkRegistry.js`, `data.reference/prompts/stages/pipeline-editorial-voice-consistency.md`, `scripts/migrations/134-editorial-voice-consistency-stage.js`)

## Series Autopilot

- **[issue-1510] Series Autopilot now catches whole-book continuity problems at the cheap beat stage, before it generates full scripts.** The autopilot reviewed continuity at two altitudes — the cross-volume synopsis pass (before beats exist) and the full-text editorial pass (after every issue's script is written, the most expensive stage) — leaving a gap in between. So structural defects spanning the whole book (a cliffhanger raised in one issue and never resolved, a finale that drifts onto a new subplot instead of landing the arc's intended ending, a promised through-line that never pays off, an event staged as the "first" in two different issues, a broken volume-to-volume hand-off) were only caught *after* all the scripts already existed — or not until a human read it. The autopilot now runs a **whole-manuscript beat-continuity pass over every issue's beat sheet, between beat generation and script generation**. Blocking findings are auto-resolved by rewriting just the affected issues' beats and re-checking (bounded, then it pauses for review with the residual findings), so the fix lands before any script is generated. The full-text editorial pass is unchanged and still catches what only the verbatim script can show (pacing, repeated/looping passages, dialogue craft). A new **Beat continuity rounds** control in the autopilot Options tunes how many auto-resolve rounds it attempts (0 skips the pass), saved as a per-install default like the existing Arc-verify and Editorial round knobs.
- **[issue-1349] Series Autopilot now refreshes its scene segmentation before running the scene-based editorial checks, so those checks read the current draft.** The autopilot's scene-aware editorial checks (scene balance, POV justification, cliffhangers, arc transitions) read a cached "reverse outline" of the manuscript's scenes — but the autopilot's own editorial-completeness pass can edit the manuscript earlier in the same run, leaving that segmentation stale. The autopilot now regenerates the reverse outline between the completeness pass and the scene-based checks, so the checks always see the freshly edited draft instead of a pre-edit snapshot. It only spends effort when there's something to do: the refresh is skipped entirely when no scene-based check is enabled, and when the stored segmentation is already current — so a run that doesn't need it costs nothing extra.
- **[issue-1514] A per-stage provider pin now wins over Series Autopilot's run provider on *every* stage, not just script verification.** Pinning a single pipeline stage to a specific provider on the Prompts page already stuck for the script-verification gate, but the autopilot still threaded its run-wide provider as a hard *override* into every other stage it drives — arc generation and verification, episode seeding, prose/comic-script text stages, volume beats, the reverse-outline refresh, the editorial checks and their auto-fixes, and manuscript completeness — so a pin on any of those was silently ignored mid-run. The autopilot now passes its run provider as a soft *default* across all of them: a pinned stage keeps running on its chosen provider, while unpinned stages still follow the run's provider. As a deliberate consequence, when the run's provider is unavailable an unpinned stage now falls through to the active provider instead of failing the run with a "provider unavailable" error. Manual "regenerate with provider X" buttons are unchanged — an explicit per-call pick still wins everywhere.
- **[issue-1558] A per-stage *model* pin now also wins over Series Autopilot's run model — completing the #1514 work.** #1514 made a pinned stage keep its chosen *provider* during an autopilot run, but the run's *model* was still threaded as a hard override, so if you launched a run with both a provider and a model and a stage had its own provider+model pin, that stage ran on the pinned provider but with the *run's* model id — which could be wrong (or invalid) for the pinned provider, and ignored the model you deliberately set on the stage. The autopilot now passes its run model as a soft *default* too, with the same precedence as the provider: an explicit per-call model pick wins, then the stage's own model pin, then the run's model, then the provider's default — so a pinned stage now keeps both its provider *and* its model, while unpinned stages still follow the run's model.
- **[issue-1571] Series Autopilot now stops a verify/editorial loop early when it stops making progress, instead of burning the whole daily budget.** The arc-verification, beat-continuity, and editorial convergence loops auto-resolve their blocking findings and re-check, bounded by a round cap. Previously a loop that wasn't actually improving — a fix that re-introduced a different break, or that just oscillated between the same findings — kept spending AI budget round after round until it hit the cap and paused anyway. The autopilot now watches the blocking-finding count across rounds and, if it stops reaching a new low for two rounds running, pauses immediately with a distinct **"not converging"** state (visible as a badge on the paused panel) so you know it needs a human edit rather than more rounds. Loops that are genuinely making progress, and runs on the default round caps, are unaffected — the early stop only kicks in once a raised cap lets a stalled or oscillating loop keep going.
- **[issue-1573] Series Autopilot now flags editorial checks that errored instead of reporting a silent "clean" run.** Each editorial check runs inside a boundary guard so one bad check (a malformed prompt, a provider timeout, a missing context) can't abort the whole pass — but a check that *threw* produced no findings, and the autopilot's per-pass broadcast always reported "0 blocking" and completed as if everything had run. A check that errored every run looked clean until someone inspected the raw per-check results. The editorial-checks round frame now carries an **errored count + the failing check ids**, and a run that finished with any errored check is qualified on the terminal summary and the persisted status banner (a caution, not "production-ready") so the partial failure is visible — the dimension that errored was never actually evaluated. The standalone "run editorial checks" panel surfaces the same errored aggregate on its completion frame. (`server/services/pipeline/seriesAutopilot.js`, `server/services/pipeline/editorial/checkRunner.js`, `server/services/pipeline/series.js`, `client/src/components/pipeline/AutopilotPanel.jsx`)
- **[issue-1574] Series Autopilot now retries a failed beats/text child run before giving up, instead of marking it attempted and silently skipping it.** When the autopilot delegates to a child runner (volume beats or per-issue text/scripts), the child can finish with its target stage still empty because the underlying LLM call failed. Previously the work was marked *attempted* before the result was inspected, so the resolver skipped it on the next pass — a failed beats run wasn't caught until a downstream stage tripped over the empty `idea`, and a failed text run only paused after the fact. The autopilot now **verifies the child actually produced its output** (every issue in the volume has a beat sheet / the issue's required script stages landed) and, on a miss, **retries the child once** (skip-existing, so the retry only fills the gap) before marking the work attempted. Each attempt is budget-gated and bills one action like any other step; if the output is still missing after the retry the run emits an escalation frame and **pauses for review with the missing stages as residual findings** rather than marching on toward a "done" with no script. A per-run **`maxChildRetries`** option tunes the retry budget (0 restores the legacy single-attempt behavior). (`server/services/pipeline/seriesAutopilot.js`)
- **[issue-1575] Series Autopilot can now run a subset of editorial checks for a single run, without touching the global enabled set.** The editorial-checks pass always ran *every* enabled check, so piloting one new check — or skipping an expensive one for a single run — meant toggling checks globally and remembering to flip them back. The autopilot start payload now accepts an optional **`editorialCheckIds`** array; when present, that run's editorial-checks pass evaluates only those check ids (unknown or disabled ids are ignored), and the same subset drives the budget gate so billing and execution agree on the set. Absent or empty falls back to the full enabled set (the unchanged default). The dry-run plan also names the subset (e.g. *"per-run subset of 2 editorial check(s)"*) instead of implying the whole enabled set will run. (`server/services/pipeline/seriesAutopilot.js`, `server/routes/pipeline/autopilot.js`)
- **[issue-1576] The Series Autopilot dry-run plan now estimates its budget cost, so a large series on a small daily cap can be spotted before it burns through text generation and never reaches editorial.** The dry-run plan enumerated the steps it *would* run but gave no sense of how much of the daily CoS action budget they'd consume. Each planned step now carries an estimated **cos-action count** — the same unit the daily cap gates on (`recordDomainUsage('cos', …)`) — leaning toward the high end: convergence loops (arc verify, beat continuity, editorial review) counted at their max rounds (verify + resolve per round), per-item steps at one action per issue, and the pure gates that bill nothing (editorial health gate, canon verify) shown as zero-cost. (The editorial review's per-comment auto-fixes bill extra and scale with the findings, which isn't knowable at plan time, so a heavy editorial pass can exceed its estimate.) The editorial-checks pass — which bills a single action but fans out to many LLM calls whose count depends on manuscript chunking — additionally carries a rough **LLM-call proxy** (*issues × enabled LLM checks*, e.g. *"~18 LLM call(s)"* for a 9-issue series with 2 LLM checks). The plan panel shows each step's `≈N act` and an **Est. budget** footer totalling the run's actions and editorial-check LLM calls, on both the start and the terminal dry-run frame. (`server/services/pipeline/seriesAutopilot.js`, `client/src/components/pipeline/AutopilotPanel.jsx`)
- **[issue-1578] Series Autopilot now streams per-check editorial progress, not just a single end-of-pass total.** The editorial-checks pass runs every enabled check across the manuscript and previously broadcast only one terminal frame — a total finding count with `0 blocking` — so during a long (issues × checks) pass the UI could show only "running" until the whole pass finished, with no signal about which check was executing or what it found. The autopilot now forwards the runner's **per-check `check:start` / `check:complete` frames** up its live progress stream, each carrying the check's label and a **high/medium/low severity breakdown** of that check's findings, so the panel shows real-time progress like *"Editorial check: Info dumping — 3 finding(s) (1H/2M/0L)"* as each check completes (errored and gated-skip checks are labeled distinctly). The same severity breakdown is now attached to each entry in the run's `perCheck` summary. (`server/services/pipeline/editorial/checkRunner.js`, `server/services/pipeline/seriesAutopilot.js`, `client/src/components/pipeline/AutopilotPanel.jsx`)

## Providers

- **[issue-1521] Claude Code on AWS Bedrock now works with a plain Claude provider — no more "model identifier is invalid" failures.** On a machine configured for Claude Code on Bedrock (`CLAUDE_CODE_USE_BEDROCK` set), pointing a standard Claude Code provider at a bare model id like `claude-opus-4-8` was rejected by Bedrock, which wants the region-prefixed form (`global.anthropic.claude-opus-4-8`) — this had broken Series Autopilot and other pipeline runs on a Bedrock box. PortOS now auto-corrects a bare Claude model id to its Bedrock form just-in-time when launching the CLI/TUI runner (preferring your `ANTHROPIC_DEFAULT_*_MODEL` setting when present, otherwise prefixing the id), so the same provider config works whether or not the box is in Bedrock mode. Your stored configuration is left unchanged (a one-time notice explains the correction), and already-prefixed ids and non-Claude providers are untouched.

## Federation

- **A peer probe with an empty or non-GUID instance id no longer spams the log with a 500 every sync cycle.** `GET /api/instances/sync-status` validated the `?forPeer=<id>` query param with `z.string().guid()` — far stricter than the `getSyncStatus()` service it feeds, which only ever uses the value as a cursor-cache key. The endpoint is reachable by any tailnet machine, and a prober running an older PortOS, carrying the `unknown` instance-id sentinel, or emitting a bare `?forPeer=` (empty string, which `.guid()` rejects) made the route throw `ServerError: Invalid GUID` on every probe — even though the sibling `/api/sync/*` checksum/snapshot routes already accept the same id leniently. The route now mirrors `dataSync.js`'s `forPeerOf` handling (trim + 128-char cap, blank/array → unscoped), so the probe degrades to the unscoped global response instead of a repeating 500. (`server/routes/instances.js`)

- **[issue-1562] New "Full mirror" mode lets a peer mirror *everything* with one toggle.** Federation used to require enabling each sync category by hand, and records that existed *before* a peer was added were never back-subscribed — so you could never be sure a node pair was a true replica. The Instances page now has a per-peer **Full mirror** switch: turning it on enables every sync category at once (shown locked-on), back-subscribes all of your existing universes, series, media collections, authors, and music records to that peer, and is reciprocated automatically so the other machine mirrors you back. It's future-proof — any new kind of record added in a later version is covered with no per-peer change — and a live **"Fully mirrored ✓ / N pending"** indicator (counted from records actually confirmed-delivered, not internal cursors) tells you the real replication state at a glance. A **"Default new peers to full mirror"** switch on *This Instance* makes every peer you add from now on start in full-mirror mode.

- **[issue-1636] Reciprocal sync (including full mirror) is now truly two-directional when one machine added the other.** When machine A added peer B and asked B to mirror back — but the user on B never explicitly added A — B adopted the sync categories yet still never pushed its *own* existing universes, series, authors, and music back to A, so the "mirror" was silently one-directional until someone on B also connected to A. B now treats A's explicit reciprocal-sync request as permission to push back and starts mirroring its existing records to A right away. Machines that mutually added each other were already fine; this only fixes the asymmetric "A added B" setup.

- **[issue-1564] Creative Director projects now federate across peers.** Creative Director projects (treatment, scenes, runs) were `db-primary` but local-only — a node pair couldn't mirror that creative work. Projects now ride the same per-record peer-sync push pipeline as Authors and Music: there's a new **Creative Director** sync category on the Instances page (and it's covered automatically by Full mirror peers), creating a project auto-subscribes it to every project-enabled peer, edits propagate as last-write-wins (newer `updatedAt` wins, with a Conflicts-tab entry archived when both sides diverged from a common base), and a delete becomes a tombstone the merge keeps an out-of-date peer from resurrecting. The project's starting image replicates as a pulled asset; scene video renders ride the project's linked media collection (federated separately). Cross-version pushes are version-gated (`creativeDirectorProjects` schema v1) so a newer peer can't corrupt an older one. (Mood Boards federation, also part of #1564, remains to ship in a follow-up.) (`server/services/creativeDirector/`, `server/services/sharing/peerSync.js`, `server/services/sharing/tombstoneGc.js`, `server/lib/schemaVersions.js`, `server/lib/syncWire.js`, `server/lib/conflictJournal.js`, `client/src/pages/Instances.jsx`)

- **[issue-1565] Writers Room works now federate across peers.** Writers Room manuscripts (works + their draft-version history) were `db-primary` (`writers_room_works` + decomposed `writers_room_draft_versions`, since #1017) but had never been wired into the cross-peer sync graph, so a node pair couldn't mirror your writing. Works now ride the same per-record peer-sync push pipeline as Creative Director projects and Mood Boards: there's a new **Writers Room** sync category on the Instances page (covered automatically by Full mirror peers), creating a work auto-subscribes it to every Writers-Room-enabled peer, edits propagate last-write-wins (newer `updatedAt` wins, with a Conflicts-tab entry archived — restorable down to title/kind/status/folder/image-style/live-mode — when both sides diverged from a common base), and a **delete becomes a soft-delete tombstone** (works used to hard-delete) the merge keeps an out-of-date peer from resurrecting. The file-primary `.md` draft prose bodies stay file-primary and replicate as pulled assets via a dedicated SHA256 body manifest (the receiver diffs each draft against its local disk and pulls only the missing/changed bodies from the sender's `/data/writers-room` mount), so prose is never round-tripped through the database record. Cross-version pushes are version-gated (`writersRoomWorks` schema v1) so a newer peer can't corrupt an older one. Folders and exercises don't federate yet (they need soft-delete columns first) — tracked as follow-ups under #1565. (`server/services/writersRoom/`, `server/services/sharing/peerSync.js`, `server/services/sharing/tombstoneGc.js`, `server/services/conflictJournalResolver.js`, `server/services/instances.js`, `server/lib/schemaVersions.js`, `server/lib/syncWire.js`, `server/lib/conflictJournal.js`, `server/index.js`, `client/src/pages/Instances.jsx`)

- **[issue-1564] Mood Boards now federate across peers (completing #1564).** Mood boards (the pinned image + text reference canvas) were `db-primary` but local-only, so a node pair couldn't mirror them. Boards now ride the same per-record peer-sync push pipeline as Creative Director projects: there's a new **Mood Boards** sync category on the Instances page (and it's covered automatically by Full mirror peers), creating a board auto-subscribes it to every board-enabled peer, edits propagate as last-write-wins (newer `updatedAt` wins, with a Conflicts-tab entry archived — restorable down to the board's `items[]` — when both sides diverged from a common base), and a delete becomes a tombstone the merge keeps an out-of-date peer from resurrecting. Image bytes a board's items reference (gallery media-keys or `/data/images/...` URLs) replicate as pulled assets; external image URLs resolve on the receiver. Cross-version pushes are version-gated (`moodBoards` schema v1) so a newer peer can't corrupt an older one. (`server/services/moodBoard/`, `server/services/sharing/peerSync.js`, `server/services/sharing/tombstoneGc.js`, `server/services/conflictJournalResolver.js`, `server/lib/schemaVersions.js`, `server/lib/syncWire.js`, `server/lib/conflictJournal.js`, `server/lib/peerSyncValidation.js`, `client/src/pages/Instances.jsx`)

- **Comic-page proof/final render buttons no longer get stuck "loading"/disabled on already-rendered pages.** Navigating into an issue's Comic tab — for example right after kicking off an unrelated render like a volume cover — could leave a page's **Proof**/**Final** buttons spinning and disabled even though that page's render finished long ago. The in-flight check re-armed an "unknown" status on every navigation and only cleared it once a job-status fetch (or a 5-second grace timer) resolved, ignoring that the slot already carried a rendered image. It now treats a slot that already has a rendered filename as settled immediately (the server clears the filename when a re-render is queued, so a present filename is an authoritative "done" signal), so the buttons are clickable right away and you can queue the next render without waiting. (`client/src/hooks/useSlotInFlight.js`)
- **The Volume covers section on a series page is now expanded by default** instead of starting collapsed, so the front/back cover editors and render buttons are visible without an extra click. (`client/src/components/pipeline/ArcCanvas.jsx`)
- **Claude Code TUI agents now reliably wait for the input prompt before sending their task (and no longer wedge the shell on startup hiccups).** TUI-provider CoS agents (e.g. `claude-code-tui`) run their CLI as a *child* of a persistent PTY shell, and the old prompt-delivery gate fired on a crude "saw output, then went idle" heuristic. During Claude Code's startup that lull happens *before* the input box exists — and on a folder it hasn't seen, claude shows a **"trust this folder?" gate** that `--dangerously-skip-permissions` does not bypass (common because agents start in fresh worktrees). The gate would paste the task into the banner / trust menu, or — if claude had already exited — into the returned shell prompt, leaving a frozen, un-typeable session showing a literal `^[[200~ …` and a silently-failed run. The spawner now (1) **waits until the shell can actually run commands** before injecting the CLI command — a fixed 200 ms delay raced a heavy interactive shell (powerlevel10k / async plugins / nvm-PATH setup), so the command ran before the env was ready and the launched TUI fell straight back to the prompt. Heuristic timing watches (a prompt-marker, or settle-on-output-quiet) are *both* fooled by powerlevel10k's *instant prompt* (it renders a fake-ready prompt — and enables bracketed-paste mode — before `.zshrc` finishes) and by slow mid-load quiet gaps (nvm/plugins) that trip a settle timer just as easily, sending the command into a half-loaded shell where the instant-prompt→real-prompt redraw swallows it (the command sits echoed-but-unexecuted at a bare prompt — the intermittent wedge). It now **proves the shell is executing commands with a round-trip probe**: it prints a unique nonce and waits to *see that nonce in the output* before injecting the command. The nonce is split in the probe source (`'PORTOSRDY' '<nonce>'`) so the command echo never contains the assembled string — only the executed output matches — and instant-prompt keystroke buffering replays the probe into the real shell, so the gate is theme- and shell-agnostic with a bounded fallback if the probe never round-trips, (2) gates the paste on claude **re-enabling bracketed-paste mode** (`ESC[?2004h`, after the shell turned it off to run the command) as the input-ready signal — this is what makes the paste's leading `ESC` read as a paste rather than as Escape (which cancels claude's input — the intermittent "something other than Enter canceled it" failure); the visible footer text (`bypass permissions on (shift+tab to cycle)`) is deliberately NOT used because claude renders it with inter-glyph cursor moves that strip its spaces after ANSI-stripping, making it unmatchable, (3) **auto-confirms the folder-trust prompt** (Enter on the default "Yes, I trust") so claims run in fresh worktrees, (4) probes that the CLI is still a live child of the shell before pasting, and (5) **never blind-pastes** — if the input prompt never appears within 45 s (or the CLI exited), it finalizes the run as a failure carrying the CLI's captured output (`tui-not-ready` / `tui-exited-early`) instead of dumping into the shell. Non-claude TUI providers keep their existing handshake. (`server/services/agentTuiSpawning.js`, `server/lib/tuiHandshake.js`, `server/services/shell.js`)
- **AudioLDM2 music generation no longer crashes on current transformers.** Rendering with the AudioLDM2 engine failed mid-generation with `AttributeError: 'GPT2Model' object has no attribute '_update_model_kwargs_for_generation'`. AudioLDM2's internal language model is a bare `GPT2Model` (the pipeline reads its hidden states), and diffusers' `generate_language_model` drives it by hand using generation helpers that used to live on every `PreTrainedModel` — but transformers ≥4.50 decoupled `GenerationMixin`, so non-generative base models like `GPT2Model` no longer carry them, leaving diffusers calling a method that no longer exists. The AudioLDM2 sidecar now re-binds the missing helpers (`_update_model_kwargs_for_generation`, `_get_initial_cache_position`) from `GenerationMixin` onto the language model after loading the pipeline — keeping the runtime on a current transformers instead of pinning an EOL release, and a no-op on older transformers where the methods are already present. (`scripts/generate_audioldm2.py`)
- **Client test suite no longer floods CI with React "not wrapped in act(...)" warnings.** Across eight component/page suites, state updates were being driven outside `act()` — hand-fired mock-EventSource frames (`emit`/`fail`), raw DOM `.blur()` calls, manually-invoked tracker callbacks, and resolved in-flight gates whose follow-up `setState` landed after the test body returned (plus a few synchronous tests whose mount-time fetches resolved late). These surfaced only under CI's verbose reporter (the default reporter hides console output), so they were invisible locally. Each driving call is now wrapped in `act()` (or switched to `fireEvent`, which wraps internally), and synchronous tests that mount components with async effects now flush inside `act()`. The full client suite (2497 tests) runs clean with zero act warnings. (`PipelineManuscriptEditor`, `Layout`, `WidgetSuggestions`, `ApiAccessTab`, `PitchTuner`, `SongTraining`, `AudioStage`, `LocalLlmPlayground` test suites)
- **Deleting a generated image or video now asks for confirmation.** The trash button on image/video preview cards (the Image Gen page's recent-renders strip and the Media History grid) deleted the asset immediately on a single click, with no undo. It now arms an inline confirm row ("Delete this image?" / "Delete this video?" with Delete + Cancel) so an accidental click can't wipe a render. Fixed once in the shared `MediaCard` component, so both surfaces are covered. (`client/src/components/media/MediaCard.jsx`)
- **Codex TUI CoS agents no longer get stuck in the sandbox.** The shipped Codex TUI provider pinned `--ask-for-approval never`, which silences approval prompts but leaves Codex's *sandbox* at its default (`workspace-write`) — so headless agents couldn't reach the network (`gh`/`npm`/`git fetch` failed) and could still hit surprise escalation prompts mid-run. Because an explicit `--ask-for-approval` flag also suppressed the auto-injected full-bypass, the shipped default actively defeated the headless posture the CLI codex path already uses. The default now ships `--dangerously-bypass-approvals-and-sandbox` (approvals off + sandbox off + network on), and migration 121 rewrites existing installs whose args still match the old default — custom Codex flags are left untouched.
- AI provider context-window planning is now model-aware: known Claude model windows are used after explicit provider overrides and before local `num_ctx`, and the provider settings UI shows/edits the planning window so mixed-model providers no longer collapse to one blunt 128K budget.
- **AI fixers can no longer take down the whole server with `pm2 kill`.** PortOS runs every managed app under one shared pm2 daemon, so `pm2 kill` / `pm2 delete all` stops *everything* — including PortOS itself. A "Fix with AI" / autofixer agent (which runs its CLI fully unrestricted) could be talked into running one while trying to "restart the app." Three layers now block this: (1) the shell-command allowlist (`validateCommand`) rejects `pm2 kill`/`startup`/`unstartup` and any `pm2 <verb> all` for the manual command runner and autonomous jobs; (2) spawned AI agents (CoS tasks, the autofixer app) get a guarded `pm2` shim prepended to their PATH that intercepts those same destructive subcommands before they reach the real pm2 — a hard guard that holds even under `--dangerously-skip-permissions`; (3) every agent prompt now states the shared-daemon rule and to only `pm2 restart <its-own-process>`. Scoped commands (`pm2 restart my-app`, `pm2 logs my-app`, `pm2 list`) still work.

## Changed

- **`claim-work` on a JIRA app now claims and ships ONE sprint ticket instead of running the broad sprint-manager triage job.** When an app's Work Tracker resolves to JIRA, the scheduled `claim-work` flow (and the `/do:next` button) used to route to `jira-sprint-manager` — which triages *every* sprint ticket and only then implements the top one as a CoS-managed run, and which left the ticket's status untouched after opening the MR. The JIRA route now uses a new per-ticket `claim-issue-jira` flow, the JIRA sibling of the GitHub/GitLab claim-issue flows: it picks the next ready ("To Do") ticket assigned to you in the active sprint, moves it **To Do → In Progress** before coding, implements it in its own `claim/<KEY>` worktree, opens an MR/PR referencing the ticket (forge auto-detected — `gh` or `glab`), then moves the ticket **→ In Review** and comments the MR/PR link. It leaves the MR/PR open for a human to land (no auto-merge). The standalone **JIRA Sprint Manager** scheduled job is unchanged and still available on its own for full-sprint triage. (`server/lib/workTracker.js`, `server/services/taskPromptDefaults/prompts.js`)
- The **Songs** workbench is now called **Rounds** (its real subject is musical rounds — Hey Ho Nobody Home, the quodlibet trio, the "500 Miles" worked example). This is a full-stack rename: the page moved from `/songs` to `/rounds`, the API from `/api/songs` to `/api/rounds`, and the on-disk store from `data/songs.json` to `data/rounds.json` (top-level `songs[]` → `rounds[]`, per-record `partnerSongIds` → `partnerRoundIds`). An auto-running migration (120) converts existing installs on the next boot/update, preserving every saved round, recording, and training-progress record — nothing to do by hand. The ⌘K palette and voice navigation still resolve "songs"/"song" to the page, so old muscle memory keeps working.

## Fixed

- Scheduled improvement tasks (documentation, security, code-quality, performance, test-coverage, accessibility, dependency-updates, ui-bugs, mobile-responsive, release-check) no longer keep an app-name-hardcoded "PortOS" prompt on existing installs. These prompts went generic (`{appName}`) a while ago, but were never versioned — so deployed instances kept the old PortOS-targeted text (and some had it mis-flagged as a user customization). They are now versioned with their prior defaults preserved, and `loadSchedule` self-heals the mis-flag, so every install auto-upgrades to the generic body on the next boot/update without clobbering genuine custom prompts.

## Fixed

- **`./setup.sh` and `./update.sh` now install ffmpeg, so camera devices, video generation, and thumbnails work out of the box.** PortOS's media/video features (`GET /api/media/devices` camera enumeration, video generation, thumbnailing, audio mux) shell out to the `ffmpeg` binary via `server/lib/ffmpeg.js` — but ffmpeg isn't an npm dependency, so a fresh install or update never provisioned it and those paths failed at runtime with `spawn ffmpeg ENOENT`. Both scripts now ensure ffmpeg is present: when it's missing they install it via the platform package manager (macOS Homebrew; Linux apt-get/dnf/pacman), fail-soft with a manual-install hint if no package manager is found. On Linux the install is prefixed with `sudo` only when not already root, so a root container (e.g. the Docker image, which has no `sudo` binary) still provisions ffmpeg. `update.sh` runs unattended (no TTY), so its Linux branch is gated on passwordless sudo (`sudo -n`) to avoid blocking on a password prompt, and surfaces an "ffmpeg" step in the Update tab's progress. (`setup.sh`, `update.sh`, `client/src/components/apps/tabs/UpdateTab.jsx`)
- **Per-issue text generation no longer lists every character twice.** Each prose/comic-script/teleplay prompt rendered the series-canon characters as full bible records in `### Characters`, and then re-listed the very same characters in the terse `### Universe at a glance` roster — so every character appeared twice in the prompt (wasted tokens, and the source of a "why is this character in here twice" report). The roster now excludes characters already in the full bible (as its own code comment always intended — it's meant to add *places/objects + other* canon not in the series bible), keeping its places/objects and any non-series-canon characters. (`server/lib/universePromptRenderers.js`, `server/services/pipeline/textStages.js`)
- **A usage-limited provider is no longer benched for up to a day.** When a CLI provider (e.g. Codex) hit its usage limit, PortOS parsed the provider's self-reported reset ("resets in 21h 52m") and benched it for that entire span — sidelining the primary for nearly a day even though usage limits reset on a rolling window (≈5h max) and capacity usually frees much sooner. The usage-limit bench is now a tight retry interval (default 10 minutes), hard-capped at the 5h reset window, and it still respects a genuinely *shorter* stated reset. The provider's stated reset is kept for display. (`server/lib/aiToolkit/providerStatus.js`)
- **Autopilot editorial auto-fix now honors the run's chosen provider/model.** The editorial loop passed the run's provider override to the manuscript *review* but not to fix *generation*, so auto-fixes silently ran on the active/default provider (and its runtime fallback) instead of the model selected for the run — which, when the default was rate-limited, degraded fixes onto a weak fallback. Fix generation now threads the same override as the review. (`server/services/pipeline/seriesAutopilot.js`)
- **Manuscript editorial review no longer reports a fully-drafted book as "empty."** The completeness pass injected the entire universe canon (characters + places + objects JSON, which for a large universe can be 800K+ chars / 200K+ tokens) as fixed context on every call. On any model whose context window the canon exceeded, the budgeter's usable-input budget collapsed to zero and the manuscript was sliced to an empty string — so the LLM "reviewed" a blank draft and returned a wall of identical "the drafted manuscript is empty" findings that could never be resolved, hard-pausing the autopilot at editorial review. The canon is now hard-capped to a reference-sized budget and window-aware-trimmed so the manuscript always keeps a budget floor (mirroring the editorial *check* runner's #1459 protection): a frontier-window model reviews the whole book in one pass, a smaller window chunks it — but the draft is never starved to empty.
- **[issue-1513] Adjacent issues no longer dramatize the same beat.** When expanding an issue's beat sheet, the model could run past the events the issue's synopsis assigns to it and pull in the *next* issue's climax — so two neighboring issues ended up staging (and climaxing on) the same arc beat. The beat-expansion prompt now treats each neighboring issue's synopsis as a hard scope boundary ("out of scope — stop at this issue's cliffhanger, not the next issue's climax") rather than as material to continue into. It also detects when an issue's synopsis is too thin for a long length profile (e.g. a one-line synopsis on a 44-page finale) — the pressure that makes the beat sheet pad by absorbing adjacent events — and tells the model to stay in scope and land fewer beats rather than overrun. Existing installs auto-upgrade the idea-stage prompt via migration 124 (customized prompts are left untouched).

## Added

- **A ▶ play button on every "To Do" card in an app's JIRA sprint board.** The sprint kanban on an app's Overview tab now puts a play button on each ticket in the **To Do** column — click it to queue a Chief of Staff agent that claims and ships *that specific ticket* (not just "the next ready one"). It reuses the same `claim-issue-jira` flow the scheduled `claim-work`/`do:next` path runs (move To Do → In Progress, implement in a `claim/<KEY>` worktree, open an MR/PR, move → In Review), but pins the agent to the ticket you picked. The task queues as a normal user task — the daemon starts it on the next evaluation. The button shows only on To Do cards (In Progress / Done tickets are already claimed or finished). (`server/routes/cosTaskRoutes.js`, `client/src/components/KanbanBoard.jsx`)
- **Context-window size on available-model cards (Settings → Local LLM).** The model cards in the install catalog and the Hugging Face search results now show each model's native context window (e.g. "128K ctx") alongside its size and parameter count — previously this only appeared on already-installed models, so you couldn't compare context windows before deciding what to install. Live Hugging Face results read the true value from the repo's GGUF metadata (`gguf.context_length`, backfilled from the same per-repo fetch that already supplies file sizes); curated catalog entries carry it when it's a documented spec for that build.
- **A per-stage provider pin now beats a blanket run-level provider default.** Pin a single pipeline stage to a specific provider on the Prompts page (e.g. run *Comic Script Verification* on Codex) and it now sticks even while Series Autopilot runs everything else on a different provider. Previously the autopilot's run-wide provider was threaded as a hard *override* that sat above stage pins, so a pinned stage was silently moved onto the run's provider. Provider resolution is now three-tiered — an explicit per-call override (a route's "regenerate with provider X") still wins, then the stage pin, then a blanket run default that only applies to unpinned stages and softly falls through to the active provider when unavailable. The autopilot's script-verify gate threads its run provider as that soft default, so a `pipeline-script-verify` provider pin is honored. (`server/lib/stageRunner.js`, `server/services/pipeline/scriptVerify.js`)

- **Music studio (Create → Music).** A new home for generating and organizing music with local OSS tools, with three tabs:
  - **Artists** — reusable musical personas (the audio analogue of Authors): name, genre, bio, musical style, plus a physical description + portrait style used to **generate an artist portrait** with your existing image-gen options, **upload** one, or pick from the gallery.
  - **Albums** — ordered track collections under an artist, with **cover art** you generate (1024×1024 via your image-gen options), upload, or pick from the gallery; add tracks and reorder them.
  - **Tracks** — singles or album members with lyrics + a generation prompt and an audio file you **upload**, **attach** from the shared music library, or **generate on-device** (see below), played back inline.

  Artists, Albums, and Tracks are db-primary PostgreSQL entities (federation-ready, mirroring Authors). Cross-peer sync for them is tracked separately (#1502); for now they're local-only.

- **On-device music generation in the Track editor.** Generate a track's audio locally from its prompt (+ lyrics) — no API key, no network at inference. Three OSS engines behind one picker: **MusicGen** (MLX) and **AudioLDM2** (diffusers) for instrumental/ambient beds, and **ACE-Step** (`INSTALL_ACESTEP=1 bash scripts/setup-image-video.sh`) for full songs *with vocals*, which uses the track's lyrics as conditioning. Engines whose opt-in venv isn't installed show their install hint and gate the Generate button (same posture as the FLUX.2 image venv). The generated WAV lands in the shared music library and attaches to the track with its engine/model/duration metadata.
- **Install additional audio models from HuggingFace.** The Track editor's generator panel can pull extra checkpoints (e.g. a larger MusicGen, an AudioLDM2 variant) straight from HuggingFace into the local cache — same streamed-download path as the image/video model installer — and they become selectable for generation immediately. User-installed models persist in `data/audio-models.json` and can be removed from the same panel.
- **Every track render is now kept and shown as a card (not just the latest).** A track used to store a single audio pointer, so re-generating (or uploading a new file) silently overwrote the previous take with no way back. Tracks now keep a full **render history** (`track.renders[]`): each generated or uploaded take is a card in the editor showing its prompt, engine, model, and duration, with an inline player. Click **Details** to open a modal with the complete prompt + conditioning lyrics, **Use** to make any past take the active one (re-points playback + the album/track audio), **Remix** to seed the generator with that take's settings for a new render, plus per-card download and delete (deleting a take leaves its bytes in the shared library — they may be shared). Existing tracks are backfilled automatically on first read: the current audio becomes the first card, no migration needed. The history federates to sync peers (every render's audio rides the asset manifest, version-gated as `tracks` schema v2 so an older peer can't silently strip it).
- **"Audio & Music" category in the Local LLM model installer (Settings → Local LLM).** The Hugging Face model search gained an audio/music output category that surfaces generation models (MusicGen, AudioLDM2, ACE-Step, Stable Audio, Magenta…) — the GGUF filter is relaxed for this category since audio models aren't GGUF. Installing an audio model downloads it into the HF cache and registers it in the **same** shared audio-model registry the Music studio reads (`data/audio-models.json`), so there's one DRY place to manage audio models: install it once from the installer and it's immediately selectable in the Music studio. Models no on-device engine can run yet — and ACE-Step, which uses a fixed checkpoint — are discovery-only with a "Visit" link. Curated suggestions lead the list: **ACE-Step v1.5**, **`google/magenta-realtime-2`**, and **`stabilityai/stable-audio-3-medium`** (the last gated behind a Hugging Face data-sharing agreement).

- **Autopilot now heals episode-level arc contradictions instead of pausing on them.** The arc-verify convergence loop re-verifies and auto-resolves the arc each round, but auto-resolve could previously only rewrite the arc + volume/season synopses — it was explicitly forbidden from touching episodes. So a contradiction that *originated* inside one episode's planning synopsis (e.g. an episode stages an event a later volume reserves as its own "first" occurrence) could never be fixed: every round re-flagged it because the episode text never changed, and the run always hard-paused with "arc verification did not converge." The auto-resolve pass can now return targeted episode-synopsis corrections (keyed by season + episode number), applied at the arc-verify gate **before** any beats/scripts are generated — so the cheap structural layer is made consistent before expensive script generation. Locked episodes are left untouched; already-expanded beats on a corrected episode are cleared so they regenerate from the fixed synopsis. (`pipeline-arc-resolve` prompt + migration 123.)
- **Autopilot can target one format of a multi-format series.** A `comic+tv` series can now be driven to just its comic draft (or just TV) in a single run via a per-run format restriction, so you don't pay to generate, say, 24 teleplays when you only want the comic proof. Absent a restriction the run authors every format the series targets (unchanged default).
- **Autopilot convergence rounds are now configurable.** The series autopilot re-verifies the arc (and re-reviews the manuscript) in bounded auto-resolve loops, and previously hard-stopped after 3 arc rounds / 2 editorial rounds with "did not converge after 3 rounds" — leaving no way to give a stubborn arc more attempts. You can now set **Arc verify rounds** and **Editorial rounds** (0–20, where 0 skips that gate) in Autonomous mode → **Options**; the values persist as a per-install default and are reused on **Resume**. A per-run override still wins over the saved default, which wins over the built-in floor (3 / 2). When a gate still can't auto-resolve within the limit, the pause message now says how many blocking findings remain and points you to edit the arc/manuscript or raise the limit and resume.
- Editorial Checks page now has an AI provider + model selector that configures the editorial pass. Leave it on "Default provider" to use the active/stage provider, or pick a specific provider/model to override the LLM-backed checks for that run.
- App **Automation** tabs now show a banner when scheduled automation is globally paused, with a one-click **Resume**. Manually clicking **Run** on a task now bypasses the global pause — the pause only stops scheduled/autonomous spawning, not explicit user triggers.
- **Vite Dev-UI host guard.** Launching a managed app's Dev UI over Tailscale used to fail with Vite's "Blocked request. This host (…) is not allowed" because Vite ≥5 rejects unknown hosts. The app detail view now checks each online Vite app's config against the host PortOS is served under and shows a warning banner when it would be blocked, with two one-click fixes: **Allow all hosts (auto)** deterministically rewrites the app's `vite.config` to `server.allowedHosts: true` (safe on a private Tailscale network), and **Fix with AI** queues a CoS agent to edit (or create) the app's `vite.config` in its own repo for configs too unusual to auto-edit. Launching by IP/localhost already works since Vite never blocks those.

## Fixed

- **Series Autopilot no longer reports a "clean complete" while blocking comic-script gaps are still filed.** The advisory script-*craft* gate (empty dialogue / missing panel descriptions from `verifyComicScript`) files a CoS gap task per offending issue but never pauses the run — so a multi-issue series could silently accumulate gap tasks, report **complete — production-ready**, and only later pause when visual rendering hit the un-renderable panels. The terminal `complete` frame and the persisted autopilot marker now carry the count of issues that filed blocking craft gaps; when any were filed, the completion toast and the status banner switch to a caution — *"Completed with N filed script-craft gap(s) — resolve before rendering"* — instead of claiming the draft is production-ready. The craft gate stays advisory (the run still completes); the terminal state just stops hiding the work that remains. (`server/services/pipeline/seriesAutopilot.js`, `server/services/pipeline/series.js`, `client/src/components/pipeline/AutopilotPanel.jsx`)
- **The Local LLM installer no longer offers Ollama a sharded GGUF it can't pull.** Picking a large quant whose GGUF is split into parts (e.g. `unsloth/Qwen3-Coder-Next-GGUF:UD-Q8_K_XL`, ~80 GB across two files) failed with Ollama's raw `400: The specified tag is a sharded GGUF. Ollama does not support this yet.` ([ollama/ollama#5245](https://github.com/ollama/ollama/issues/5245)). The Hugging Face catalog already detected multi-part shards (it sums their sizes) but still presented them as Ollama-installable. Each variant now carries a `sharded` flag; on the Ollama backend a sharded quant is marked unsupported, the quant picker tags it `· sharded (not on Ollama)`, the **Install** button is disabled with the reason, and the RAM-aware default skips it so it never auto-selects a build that can't be pulled. LM Studio is unaffected — it loads sharded GGUFs natively, so the same quant stays installable there. As defense-in-depth for the pull-by-name path, an Ollama pull that hits the sharded-GGUF 400 is now tagged `code: SHARDED_GGUF` and surfaces an actionable toast ("pick a smaller single-file quant, or install on LM Studio") instead of the raw error. (`server/services/huggingFaceCatalog.js`, `server/services/ollamaManager.js`, `client/src/components/settings/LocalLlmTab.jsx`)
- **The `/do:next` Slashdo button now honors the app's Work Tracker.** Per-app Work Tracker (PLAN.md / GitHub / GitLab / JIRA) already routed the *scheduled* `claim-work` flow, but the manual `/do:next` button inlined the raw slashdo command body — which always drains PLAN.md unless `--issues` is passed. Clicking it for an app set to GitHub/GitLab/JIRA Issues silently worked the wrong queue. The button now resolves the app's Work Tracker and delegates to the same claim flow the scheduler uses (`plan-task` / `claim-issue` / `claim-issue-gitlab` / `claim-issue-jira`, via a shared `buildClaimWorkTask` router that also drives the scheduled path), each of which self-manages its own worktree + MR/PR. It also resolves the same configured `claim-work` metadata the scheduler does (global → per-app override → Code Review Defaults), so clicking it honors the app's `issueAuthorFilter` (e.g. claim any open issue, not just owner-filed) and configured reviewers instead of silently forcing owner-only + Copilot. The task description and button tooltip now read generically ("claim the next work item per this app's Work Tracker") instead of naming PLAN.md.
- **The GitHub Release workflow no longer crashes on a large changelog.** The v2.22.0 release (a ~175KB changelog) failed at the "Create Release" step with `Argument list too long`: the changelog was passed to `softprops/action-gh-release` as the `body:` input, which GitHub materializes as an `INPUT_BODY` environment variable, and a 175KB env var overflows `ARG_MAX` when the action's Node process is exec'd. The body also exceeds GitHub's hard 125,000-character release-note limit (a separate HTTP 422). The workflow now writes the (placeholder-substituted) changelog to a file and feeds it via `body_path:` instead of an env var, and caps it at 120,000 chars on a whole-line boundary — appending a pointer to the full changelog file at the tag when truncated. (`.github/workflows/release.yml`)
- Scheduled-task descriptions in the Automation UI no longer fall back to a raw dasherized label. `claim-work` (the single-source work-tracker router), `code-reviewer-a`/`code-reviewer-b`, and `do-replan` now show real one-line descriptions instead of "claim work" / "code reviewer a", which read as orphaned/legacy tasks. A parity guard test now keeps the description map in sync with the task-type list.
- PM2 standardization no longer assigns ports that are already in use. The analysis now feeds the AI the set of listening/reserved ports (plus the common Vite defaults 5173/5174) to avoid, and a deterministic post-pass reassigns any port that still collides into the 6000–6099 range — so standardizing an app never lands on a port another app already holds.
- Editing an app's ports now persists. Port fields are derived from `ecosystem.config.cjs`, so saving only updated PortOS's registry while PM2 kept the old ports and the next config refresh reverted the change. Saving a changed port now rewrites the matching value in the app's `ecosystem.config.cjs` (the source of truth). Restart the app for the new port to take effect.
- Editing a **shared or derived** port now persists too. When two port fields resolve to the same literal (e.g. the prod UI is served by the API, so `uiPort === apiPort`), the value-keyed rewrite couldn't tell them apart and the edit returned a 422. The PUT route now falls back to a **per-process-label-targeted** rewrite: it locates the specific process block in `ecosystem.config.cjs` and rewrites only the touched label's port (`api`/`ui`/`devUi`), leaving the sibling field — and any same-valued unrelated number — untouched. Edits to ports that live only under a process's `env`/`args` (no top-level field) persist as well, and an explicit `ports: { ui: N, devUi: N }` map with matching values is split correctly (only the edited key changes). A save that mixes a distinct port and a shared port is written **atomically** — both rewrites are computed in memory and the config file is written once, only when every edit is persistable — so a request that's rejected (422) never leaves the config half-changed. The 422 is reserved for the genuinely unpersistable case (the literal isn't in the touched process block — e.g. it lives in a shared `const PORTS = {…}` referenced via `ports: PORTS.server`, or is computed entirely at runtime), so a shared-value edit never falsely reports success while the displayed port silently reverts. For an app whose prod UI is **served by the API** (no separate UI port), the UI port can't be set on its own — it always follows the API port: the stored UI port is normalized to track the API port on every save (so it never freezes at a stale value), and you change it by changing the API port.
- **The character-name dissimilarity check no longer floods an epithet cast with thousands of false "confusable name" findings.** Two bugs compounded on a cast that uses article-led epithet names ("THE CABARET SINGER", "THE CHAPTER SEVENTEEN READER", …): (1) the check compared on the raw name, so 60+ "THE …" names all read as starting with the same letter and sharing the same 3-character opening; and (2) "same first letter" and "same opening" were counted as two independent similarity signals even though a shared 3-char opening always implies a shared first letter — so any two names sharing a prefix auto-reached the default 2-signal flag threshold. On one 24-issue series this produced ~2,100 spurious naming findings (and a "44 of 69 names start with T" crowding finding that was really just the article "THE"), driving the editorial health score to 0 and hard-pausing Series Autopilot at the health gate. The check now strips a leading article (the/a/an) before comparing names and counts the first-letter/opening overlap once, so it flags names readers would genuinely confuse and an epithet cast is judged on its real distinguishing words. (`server/lib/editorial/nameSimilarity.js`, `server/lib/editorial/checkRegistry.js`)
- **Deterministic editorial checks now self-heal — corrected or resolved findings clear instead of lingering forever.** The editorial-checks pass seeded every finding in `merge` mode, which only ever *adds* findings: an open finding a deterministic check no longer produces (because the check was fixed, or the underlying canon/manuscript changed) stayed open permanently. That let stale findings accumulate and could keep Series Autopilot's editorial-health gate blocked on problems that no longer exist. Deterministic checks (pure functions of their inputs) are now seeded in `fresh` mode scoped to each check's own id, so a finding the check no longer surfaces is auto-dismissed (a sync-safe status flip, never a deletion) — including when a check finds nothing at all that pass. LLM checks stay `merge`-only, since an absent LLM finding can be sampling variance rather than a real resolution; one check's reconciliation never touches another check's or the completeness pass's open comments. (`server/services/pipeline/editorial/checkRunner.js`)
- **The name-dissimilarity check no longer flags a name against its own plural.** A role-noun cast ("THE POET" / "the poets", "a reader" / "readers", "a waiter" / "the waiters") tripped the check's edit-distance-1 rule — a single trailing "s" reads as a near-identical-spelling typo — and escalated each pair to a *high*-severity blocker, even though singular-vs-plural is a deliberate count distinction (an individual vs a crowd) a reader parses effortlessly. The check now recognizes singular/plural variants of the same noun (the three common English inflections: -s, -es, -ies) and treats the pair as inert, while two genuinely different words that merely both end in "s" still compare normally. (`server/lib/editorial/nameSimilarity.js`)
- **A season synopsis is no longer truncated mid-sentence (which could hang Series Autopilot's arc-verify loop).** The season-synopsis cap was 4000 characters — half the arc-level summary cap (8000) — even though a season synopsis covers a whole season's worth of episodes. A full synopsis got hard-clipped mid-word, and because the arc-verify→resolve convergence loop re-flags a mid-sentence truncation while the resolver keeps regenerating an over-cap synopsis that gets re-clipped, the loop burned all its rounds and paused without ever converging. The season-synopsis cap is now 8000 (matching the arc summary), so a complete synopsis is stored intact. (`server/lib/storyArc.js`)
- **Arc prose fields (logline, synopsis, ending hook) now clip on a sentence/word boundary instead of mid-word.** A hard character-cap `slice()` could clip a season logline or synopsis mid-word ("…starts tracing the brand and"), which the arc-verify pass flags as "truncated mid-sentence" — and because the resolver then regenerates an over-cap value that gets re-clipped identically, the verify→resolve loop could never converge and paused the autopilot. A new `trimToClause` helper backs an over-cap value off to the last sentence terminator within budget (or, for a run-on, the last whole-word boundary), so a capped field always reads as complete prose. Applied to the arc and season logline/summary/synopsis/ending-hook/protagonist-arc fields. (`server/lib/storyBible.js`, `server/lib/storyArc.js`)
- **Hugging Face model search now fills category tabs with no search term.** Clicking a category tag (Coding, Vision, Lightweight, Multilingual) on the Local LLM install page with an empty search box rendered a blank list. The default browse phrase for each category was a multi-word string ("coding coder agentic code gguf", "small 4b 3b 2b gguf"), but the Hub `search` param AND-matches every space-separated token against the model id — and no repo's id contains all of them, so the query returned zero results. Each category now seeds a short keyword + "gguf" phrase ("coder gguf", "vision gguf", etc.) that reliably returns the top-downloaded models for the category, and "Lightweight" uses "1b gguf" so it surfaces genuinely small models instead of `Mistral-Small-24B`. (`server/services/huggingFaceCatalog.js`)

## Internal

- **[issue-1640] Extracted the duplicated gallery-image filename resolver into one shared `server/lib/localImageFilename.js` helper.** Four federated-record domains each carried a byte-identical copy of the "resolve a stored image reference to its bare `data/images/` filename (or null for empty/external-URL/non-image-path)" logic that the peer-sync asset pipeline relies on — `headshotImageFilename` (authors), `portraitImageFilename` (artists), `coverImageFilename` (albums), and `startingImageFilename` (Creative Director projects). They now all delegate to a single `localImageFilename(urlOrPath)` (barrelled + cataloged per the module-organization rule); each domain keeps its named export as a thin wrapper so the imports `peerSync.js` relies on are unchanged. Pure-function refactor, no behavior change; the new helper is unit-tested and each domain's existing `logic.test.js` still pins the wrapper behavior. (`server/lib/localImageFilename.js`, `server/services/{authors,artists,albums,creativeDirector}/`)
- **[issue-1642] Mood Board's image-asset resolver now builds on that same shared helper instead of carrying its own copy.** The mood-board `imageUrlToAppAsset` (the `{ kind, filename }` superset that also resolves `/data/image-refs/`) reimplemented the identical "trim → reject external URL → strip querystring → basename" pipeline that `localImageFilename` already owns. It now delegates the gallery-image and rejection cases to `localImageFilename` and shares a new `assetBasename` primitive (the strip-querystring→basename step) for the image-refs branch, so the last copy of that logic is gone. Pure-function refactor, no behavior change — the existing `moodBoard/logic.test.js` cases all still pass and `assetBasename` is unit-tested. (`server/lib/localImageFilename.js`, `server/services/moodBoard/logic.js`)
- **[issue-1524] Renumbered the duplicate `121-comic-script-visible-text-prompt` migration to `126`.** A rebase had landed two migration files numbered 121 (`121-codex-tui-bypass-sandbox` and `121-comic-script-visible-text-prompt`) on main. Both still applied exactly once (migrations are keyed by full filename, not number), so this was cosmetic — but the duplicate number muddied log/sort order. Renamed the comic-script one to the next free number; it's an idempotent hash-driven prompt-replace, so installs that already applied it re-run it harmlessly as a no-op. (`scripts/migrations/126-comic-script-visible-text-prompt.js`)
- **[issue-1577] Added a drift guard tying the Series Autopilot dry-run plan to the resolver.** `buildDryRunPlan` (the steps the autopilot advertises it WILL run) is kept in sync with `resolveNextStep` (the steps execute actually runs) **by hand** — a future edit to the resolver could leave the plan advertising steps execute will skip, or vice-versa, with no test to catch it. A new parity test walks `resolveNextStep` the way the execute dispatch does (applying each completed step's effect and re-resolving) to collect the ordered step sequence, then asserts it matches `buildDryRunPlan`'s output across the comic+visual, text-only, editorial-rounds-0, and TV fixtures. Test-only; no behavior change. (`server/services/pipeline/seriesAutopilot.test.js`)
